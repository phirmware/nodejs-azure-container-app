name: start automatic deployment for triggr app on main

on:
  push:
    branches:
      - main

jobs:
  curl:
    runs-on: ubuntu-latest
    env:
      BEARER_TOKEN: ${{ secrets.BEARER_TOKEN }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          ref: main
      - name: Perform curl request
        run: |
         RESPONSE=$(curl -H "Authorization: Bearer $BEARER_TOKEN" -H "X-Commit-SHA: $GITHUB_SHA" -s -o /dev/null -w "%{http_code}" https://4248-102-88-62-49.eu.ngrok.io/api/v1/provider/webhook/triggr/automaticDeployment)
          if [[ "$RESPONSE" != 2* ]]; then
            echo "Request failed with status code $RESPONSE"
            exit 1
          fi